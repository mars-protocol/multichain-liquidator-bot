import { Network } from '../../types/network';
import { RedbankExecutorConfig } from '../RedbankExecutor';
export declare const getConfig: (liquidatorMasterAddress: string, network: Network) => RedbankExecutorConfig;
