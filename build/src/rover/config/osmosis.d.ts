import { Network } from '../../types/network';
import { RoverExecutorConfig } from '../RoverExecutor';
export declare const getConfig: (liquidatorMasterAddress: string, network: Network) => RoverExecutorConfig;
